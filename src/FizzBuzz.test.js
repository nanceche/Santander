import { render, screen } from "@testing-library/react";

test("When the integer is divisible by 3 the endpoint returns Fizz", () => {});

test("When the integer is divisible by 5 the endpoint returns Buzz", () => {});

test("When the integer is divisible by both 3 and 5 the endpoint returns FizzBuzz", () => {});

test("When the integer is not divisible by 3 or 5 the endpoint returns the interger", () => {});
